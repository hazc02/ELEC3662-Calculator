#ifndef KEYPAD_H
#define KEYPAD_H

/**
 * @brief Scans the keypad and returns the character corresponding to the pressed key.
 * @return The character of the pressed key, or '\0' if no key is pressed.
 */
char Keypad_GetKey(void);

#endif 
